
\documentclass{article}

% Load required packages and commands for propper richtext handling
{% include 'wagtail_preamble.tex' %}

%% You can set width for the richtext image insertion
%
% {% raw %}
% \renewcommand{\fullwidth} {0.8\textwidth}
% \renewcommand{\partialwidth} {0.5\textwidth}
% {% endraw  %}


\begin{document}

% Set the documents 

\author{ {{-object.author-}} }
\date{ {{-object.creation_date-}} }

% this is the default title, you can simply overwrite it
\title{ Demo }

\maketitle

{% for block in object.content %}
    {% if block.block_type == 'heading' %}
        \section{ {{- block.value -}} }
    {% else %}
        {% include_block block %}
    {% endif %}
{% endfor %}

\end{document}
